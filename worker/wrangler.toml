name = "ofertas-voo-agent"
main = "index.js"
compatibility_date = "2026-06-21"

# A GROQ_API_KEY é um Secret do Worker — definida via `wrangler secret put GROQ_API_KEY`
# (ou no Cloudflare Dashboard). Nunca declarar como variável aqui.
